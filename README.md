
## Подготовка проекта к запуску

Предполагается, что ***GIT*** и **_Node.js_** уже установлены на вашей машине глобально. Если нет, то скачиваем и выполняем установку.
Все команды необходимо набирать в консоли, находясь в корневой папке проекта.

Выполняем из консоли следующую команду (*точка в конце обязательна*):
```
> git clone https://github.com/gitolog/ss-toolkit.git .
```
При этом в корень проекта будут скопированы файлы **_gulpfile.js_** и **_package.json_**,необходимые для дальнейшей установки. 

### 1. Устанавливаем Gulp
Если Gulp уже установлен, этот пункт можно пропустить.
```
> npm install -g gulp
```
Ключ **-g** говорит о том, что устанавливается системно.

### 2. Устанавливаем необходимые модули Gulp

```
> npm install --save-dev
```

Будут установлены модули, перечисленые в файле **_package.json_**:

### 3. Работа с проектом. Запуск задач Gulp

Все команды набираются в окне терминала IDE WebStorm или PhpStorm

#### Общий BUILD всего тулкита
```
> gulp build
```
* Выполнится полная очистка папки **_/build_**

* Выполнится компиляция всех файлов **_*.styl_** из папки **_/src_**, превратив их в CSS и переложит в папку **_/build/css_**

* Выполнится минификация файлов **_*.css_** в папке **_/build/css_**

* Выполнится компиляция всех файлов **_*.js_** из папки **_/src_**. Скрипты будут объединены в один файл, который окажется в папке **_/build/javascript_**

* Выполнится минификация файлов **_*.js_** в папке **_/build/javascript_**

#### Задача быстрой компиляции (слежение за файлами)
```
> gulp
```
Эта задача запускается для отладки при внесении изменений в исходные файлы стилей и скриптов тулкита. Результат компиляции будет перезаписывать выходные файлы в папке **_/build_**

* При изменении любого из файлов **_*.styl_** в папке **_/src_** выполнится сборка CSS и их минификация в папку **_/build_**, а так-же копирование в папку **_/www/css_**

* При изменении любого из файлов **_*.js_** в папке **_/src_** выполнится сборка JS и их минификация в папку **_/build_**, а так-же копирование в папку **_/www/javascript_**


##### Live reload окна браузера
Для автоматического обновления окна браузера во время редактирования скриптов, стилей и шаблонов необходимо установить плагин LiveReload:

[Плагин для Google Chrome] (https://chrome.google.com/webstore/detail/livereload/jnihajbhpnppcggbcgedagnkighmdlei)

[Плагин для Mozilla Firefox] (https://github.com/downloads/siasia/livereload-extensions/LiveReload-2.0.9.xpi)